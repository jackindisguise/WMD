/**
 * Enum for message categories.
 * @enum {string}
 */
const MessageCategory = {
	DEFAULT: "DEFAULT",
	WHISPER: "WHISPER",
	CHAT: "CHAT",
	SAY: "SAY",
	ACTION: "ACTION"
};

module.exports = MessageCategory;
